//cấu hình một cột của table
const columnConfig = {
    title, //tiêu đề của cột
    titleFull, //tiêu đề không viết tắt => hover vào sẽ hiện đầy đủ
    key, // key để map với dữ liệu trong database
    titleAlign, //căn lề cho header
    columnAlign, //căn lề cho dữ liệu trong bảng
    width, //chiều rộng của cột
    dataType, //loại dữ liệu trong cột
    visible: true, //thuộc tính ẩn hiện cột


}