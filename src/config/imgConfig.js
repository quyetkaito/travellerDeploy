const imgConfig = {
    src: 'String', // thường là đường link ảnh, nếu là local thì bỏ qua phần @assets/
    format: 'String', //định dạng file ảnh
    type: 'local|link', //xác định xem ảnh là local hay là link online
}